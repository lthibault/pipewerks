package net

import net "github.com/lthibault/pipewerks/pkg/net"

type (
	// Addr is the standard library net.Addr
	Addr = net.Addr
	// Error is the standard library net.Error
	Error = net.Error
	// Transport is the pipewerks net.Transport
	Transport = net.Transport
	// Listener is the pipewerks net.Listener
	Listener = net.Listener
	// Conn is the pipewerks net.Conn
	Conn = net.Conn
	// Streamer is the pipewerks net.Streamer
	Streamer = net.Streamer
	// Stream is the pipewerks net.Stream
	Stream = net.Stream
)
